/* ---------------------------------------------------
PARTICLES.JS LOADER (SAFE FOR GITHUB PAGES)
--------------------------------------------------- */

particlesJS.load(
"particles-js",        // The ID of the container
"particles.json",       // The config file (must be in same folder)
function () {
console.log("Floating particles loaded successfully.");
}
);
